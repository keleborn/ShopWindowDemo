server:
  port: 8081

spring:
  main:
    web-application-type: reactive
  r2dbc:
    url: r2dbc:h2:mem:///serveraccount?;B_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always